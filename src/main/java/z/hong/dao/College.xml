<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org./dtd/mybatis-3-mapper.dtd">
<mapper namespace="z.hong.dao.CollegeDao">
    <!--按排名查询-->
    <select id="selectCollegeRank" parameterType="Integer" resultType="college">
    SELECT
        college_id,
        college_name,
        college_location,
        art_min_grade,
        art_min_rank,
        science_min_grade,
        science_min_rank
    FROM
        college
    where
        science_min_rank > #{rank}
        
    </select>
    <!--按分数查询-->
    <select id="selectCollegeGrade" parameterType="Integer" resultType="college">
    SELECT
        college_id,
        college_name,
        college_location,
        art_min_grade,
        art_min_rank,
        science_min_grade,
        science_min_rank
    FROM
        college
    where
        science_min_grade > #{grade}
    </select>
</mapper>